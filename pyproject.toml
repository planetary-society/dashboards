[project]
name = "nasa-data-dashboards"
version = "0.1.0"
description = "Interactive NASA spending and contract data dashboards"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32.5",
]
